/* pages/canvas/canvas.wxss */
.canvas{
   position: absolute;
   top:0;
   z-index:1;
}

.photo{
   width:388rpx;
   height:78rpx;
}
.photo-box{
   left: 182rpx;
   bottom: 56rpx;
   position: absolute;
   z-index:9;
}

/* 画布绘制过程不能隐藏 但是又不能被用户看见 所以定位在用户看不到地方 */